# 四个工具都没有检查的合规要求

本文为这个包而写，并非为这个代码树而写。每一项都说明了什么没有被检查、为什么没有被检查，以及——在相关处——一个有真实案例的事件：如果有一个检查，本可以拦下它。

其中三项被记录为正式条目（`09-absent`），因为某个具体条款没有任何消息（message）。其余各项列在这里，是因为**没有任何逐证书的 Linter 能回答它们**：证据在证书之外。这个区分正是本列表的用意所在。

---

## A. 四个工具中没有任何一个给出消息的条款

**BR § 7.1.4.3，逐字符一致。** 从属 CA 的 Subject 属性必须与签发 CA 的 Subject"逐字符（character-for-character）"一致。没有任何工具对此发出任何消息；其中有两个工具在路过时会解码这两个 DN。记录为 `CT-039`。

**BR § 6.1.5，从属 CA 的析取（disjunction）。** 密钥大小这一列是一个析取式，只有其中一支被 lint，因此一支从属 CA 若走了另一支，便无人审查。记录为 `ZT-094`。

**一张完全没有 `keyUsage` 的订阅者证书。** BR § 7.1.2.7.6 将其定为 SHOULD；没有任何工具报告它。记录为 `ZT-068`。

**RFC 5280 § 4.2.1.10 名称约束（name constraints），真正被应用的情况。** 每个工具都检查了 `nameConstraints` 扩展的*语法*。但没有一个检查"在技术上受约束的 CA 之下签发的证书是否落在该约束范围内"，因为这需要签发者的证书。

---

## B. 只有借助签发者或证书链才能回答

一个只接收单文件的 Linter 看不到这些。而保留证书链的监控器（monitor）可以看到。

**交叉证书豁免。** MRSP § 5.3 要求 2019-01-01 之后创建的中间证书带有 EKU，并豁免了与根证书共享私钥的交叉证书。证书本身不体现这种"共享"，因此四个工具要么把每个这样的中间证书都报出来，要么一个都不报。要判定这一点，需要一个以 Subject 和公钥为键的披露数据库。

**BR § 7.1.2.2 的前置条件。** "与一个或更多现有 CA 证书相同的 Subject Name 和 Subject Public Key Information。" 这两个字段都在其他证书里。

**针对签发者的签名验证。** 这里的工具没有一个验证该证书是否由它所声称的密钥签名。对一段被篡改过的 TBS（To-Be-Signed）的签名，在本包的每个检查之外。

**路径长度算术。** `pathLenConstraint` 只被检查了编码是否正确，从未对照它之下实际签发的深度来校验。

---

## C. 只有跨某个签发者的全体产出才能回答

单张证书本身不可能"异常"。

**序列号熵，可度量。** BR § 7.1 要求 64 位的 CSPRNG 输出。每个工具都检查了序列号的*长度*。但长度不等于熵：一个 CA 若发出固定前缀加 32 位计数器，能通过每一个现有的检查。可度量的版本是统计某个 issuer 产出中"曾经变化过的比特位"，而这需要该 issuer 的产出。

**共享的 RSA 因子。** 对全部已签发的模数做两两 GCD，能找出用坏掉的 RNG 生成的密钥。批处理 GCD 使之可行；任何 Linter 都做不到，因为这是"集合"的属性。

**签发画像（issuance-profile）偏离。** 一个 CA 签发的 DER 形状——标签骨架、扩展顺序、策略标识符、critical 位——在每个签发 CA 那里都极其稳定。某 CA 在一年来只发一种形状之后，第一次出现全新形状，值得关注，而任何"拿单张证书对照文档"的工具都看不见。

**CRL 分片披露。** 一个不在签发者已披露 CRL 列表上的 `cRLDistributionPoints` URL，是披露失败。这需要披露列表，而去拉取该 URL 不是办法——那是攻击者选定的。

---

## D. 只能从日志（log）中回答

**伪造的 `notBefore`。** 在单张证书内，唯一的反证是一个内嵌的 SCT，而把两者比较是个普通的 lint——zlint 有它，并且 `ZT-011` 记录道：该 lint 以它所要监管的字段本身作为开关条件，因此一张被大幅回拨日期的证书反而不会被它判定。对照日志，这个问题很简单：条目大致按到达顺序追加，所以一个明显跳离其邻居的 `notBefore` 极为醒目，且不需要任何第二来源。

**SCT `leaf_index` 扩展。** static-ct-api 日志会给 SCT 附加一个 `leaf_index` 扩展。CA 既曾弄坏它——把 base64 文本塞进二进制字段——也曾剥离它，重新编码一个 SCT 时丢掉了日志签名所覆盖的扩展，于是它无法验证。检测"被剥离"的情况，需要知道哪些日志会发出扩展，而这可以从流量中学到，没有任何 Linter 包含这一点。

---

## E. 弱密钥，其中两个工具根本不看

`zlint` 带有一条 Fermat 因式分解 lint。`pkilint`、`x509lint`、`certlint` 对密钥质量则完全没有检查——它们检查的是密钥*大小*，那是另一个问题。已通过对每个固定代码树做 grep 验证。

其中两项有独立的二进制可用（`dwklint` 对应 Debian 弱密钥列表，`rocacheck`），因此一个运行了整套套件的部署可能已经覆盖。而只单独运行这四个工具之一的用户则没有覆盖。

- **ROCA**（CVE-2017-15361）。Infineon RSALib 指纹是对模数做一次廉价的常数时间测试。记录于此，因为它是最清晰的例子：一项检查代价低、有公开测试向量，却在四个工具中的三个里缺失。
- **相近的素因子。** Fermat 方法能找出那些素数彼此相邻的密钥——这是一整类坏掉的生成器。
- **跨签发者的过小或重复模数。**

每一项都有真实案例：2017 年的 ROCA 披露迫使爱沙尼亚和斯洛伐克的国家身份证证书被替换；而可被 Fermat 因式分解的密钥，在公开信任的签发中不止一次出现过。

---

## F. 两项规范本身使之不可判定，列于此以免有人重复推导

**位于公共后缀（public suffix）左侧的通配符** 是允许的，前提是申请者证明了对该后缀的控制权。证书里没有任何内容说明他们是否做到了。

**组织名是否准确。** 对它的每一项检查都是格式检查。这是个验证（validation）问题，而非证书问题，没有任何 Linter 应当声称能判定它。
